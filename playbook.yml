- hosts: localhost
  connection: local
  vars:
    dbuser: "testuser"
    dbpass: "badpasss"
    dbname: "testdb"
  tasks:
    - name: Install git
      apt:
        name: git
      become: yes

    - include: postgres.yml

#    - name: Check if ruby is installed
#      stat:
#        path: /usr/local/bin/ruby
#      register: chk_ruby
#
    - include: ruby.yml
#      when: not chk_ruby.stat.exists
#
#    - include: rails.yml
